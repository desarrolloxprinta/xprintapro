# Tareas: Rediseño de Página de Inicio

- [ ] 1. Auditar `src/main.js` y extraer todos los estilos inline (`style="..."`) generados previamente en la fase rápida.
- [ ] 2. Crear las clases correspondientes en `src/style.css` (ej. utilities de padding, margin, max-width, flexbox).
- [ ] 3. Refactorizar el renderizado del `main.js` para usar únicamente estas clases.
- [ ] 4. Crear la cuadrícula base para la sección de "Sectores" (Automoción, Banca, etc.) en `main.js` y estilizarla usando las reglas globales.
- [ ] 5. Validar cumplimiento constitucional (verificación de estilos hardcodeados) mediante inspección manual.
